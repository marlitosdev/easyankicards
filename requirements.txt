# Interface de desktop (janela nativa). Opcional: sem ela, o app abre no navegador.
pywebview>=5.0
# Converter arquivos (PDF, Word, Excel...) em texto no desktop
markitdown[all]>=0.0.1
# Uso por linha de comando (cli.py) — geracao de .apkg em Python
genanki>=0.13
